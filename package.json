{
  "name": "stuff",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@types/webextension-polyfill": "^0.12.1",
    "esbuild": "^0.23.1",
    "web-ext-types": "^3.2.1",
    "webextension-polyfill": "^0.12.0"
  },
  "dependencies": {
    "nodemon": "^3.1.4",
    "typescript": "^5.6.2"
  },
  "scripts": {
    "build": "node build.js",
    "start": "web-ext run",
    "dev": "npx nodemon -w ./src -e ts --exec node build.js"
  }
}
